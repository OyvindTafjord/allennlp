description: Friction Q SP Elmo+UniLSTM BS=10, blankblank
tasks:
  - spec:
      blueprint: allennlpfrictionqV9
      resultPath: /output
      args:
        - python
        - '-m'
        - allennlp.run
        - train
        - /config.json
        - '-s'
        - /output
        - '--file-friendly-logging'
      datasetMounts:
        - datasetId: ds_olfir2zkzqd5
          containerPath: /inputs/friction-q-v3x
        - datasetId: ds_gnguunokqdaf
          containerPath: /glove
        - datasetId: ds_o9d39otkaz2e
          containerPath: /config.json
      requirements:
        gpuCount: 1