description: Friction Q SP World tagged as extractions
tasks:
  - spec:
      blueprint: allennlpfrictionqV28
      resultPath: /output
      args:
        - python
        - '-m'
        - allennlp.run
        - train
        - /config.json
        - '-s'
        - /output
        - '--file-friendly-logging'
      datasetMounts:
        - datasetId: ds_x3kx7epmfev0
          containerPath: /inputs/friction-q-v3xlwe
        - datasetId: ds_crvqcd2mpm2v
          containerPath: /glove
        - datasetId: ds_2zr5renge0nc
          containerPath: /config.json
      requirements:
        gpuCount: 1